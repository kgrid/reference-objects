from setuptools import setup, find_packages

VERSION = '0.1.0' 
DESCRIPTION = 'Python Knowledge Object Package'

# Setting up
setup(
        name="python_hello_world_v1_0", 
        version=VERSION,
        description=DESCRIPTION,
        packages=find_packages()
)